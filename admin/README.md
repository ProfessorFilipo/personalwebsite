# /admin

Painel administrativo, protegido por login (tabela `admin_users`).
CRUD para: pages, research_projects, experience, courses, blog_posts, media.

- `includes/` — lógica específica do admin (auth, upload handler)
- `templates/` — views do painel (formularios, listagens)
