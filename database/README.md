# /database

- schema.sql — criação de todas as tabelas (rodar uma vez, na configuração inicial)

Para aplicar: veja as opções no README principal do projeto (PhpStorm Database tool,
phpMyAdmin, ou script PHP de migração via FTP).
